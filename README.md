# Explain-My-Config
A simple, open-source CLI tool that reads configuration files and outputs plain-English explanations. Supports .env, JSON, and YAML formats with 60+ built-in key definitions and smart fallback for unknown keys.
